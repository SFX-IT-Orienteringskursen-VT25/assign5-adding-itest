{
  "name": "storage-api-with-db",
  "version": "1.0.0",
  "description": "RESTful API with MS SQL database and integration tests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --setupFilesAfterEnv=./test/setup.js",
    "test:watch": "jest --watch --setupFilesAfterEnv=./test/setup.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mssql": "^9.1.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.0.1",
    "testcontainers": "^10.10.0"
  }
}